# MongoDB connection string
MONGODB_URI=mongodb://localhost:27017/promptpal

# JWT Secret for token generation and verification (generate a secure random string)
JWT_SECRET=your_jwt_secret_here

# Port for the server (default: 5000)
PORT=5000
